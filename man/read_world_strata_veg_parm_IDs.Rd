% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/worldfile_utils.R
\name{read_world_strata_veg_parm_IDs}
\alias{read_world_strata_veg_parm_IDs}
\title{Read stratum veg IDs and hierarchy IDs from a worldfile}
\usage{
read_world_strata_veg_parm_IDs(worldfile)
}
\arguments{
\item{worldfile}{Path to a RHESSys worldfile.}
}
\value{
A data frame with columns: `basin_ID`, `hillslope_ID`, `zone_ID`,
  `patch_ID`, `stratum_ID`, `veg_parm_ID`.
}
\description{
Fast parser for RHESSys worldfiles that extracts only the hierarchy IDs
(`basin_ID`, `hillslope_ID`, `zone_ID`, `patch_ID`, `canopy_strata_ID`) and
`veg_parm_ID`, returning one row per stratum (`veg_parm_ID` line).
}
\details{
This avoids constructing the full world table and is substantially faster and
lower-memory than `read_world()` for this specific use case.
}
